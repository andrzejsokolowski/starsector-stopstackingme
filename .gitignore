# Gradle
.gradle/
build/

# Claude / planning scratch
.claude/

# Compiled output
out/
bin/

# IntelliJ
.idea/
*.iml

# NOTE: gradle.properties IS tracked (like Hullmods - Renewed) because it holds the
# build-critical `kotlin.stdlib.default.dependency=false` flag. If you fork this and your
# Starsector lives elsewhere, just edit `starsectorPath` in it.

# Built jar is committed on release tags only; ignore during normal dev to avoid churn.
jars/

# Staging folder for the release archive (regenerated by the packaging step)
/StopStackingMe/

# Packaged release archive (regenerated on each version cut)
*.zip
